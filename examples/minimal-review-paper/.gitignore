# Build artifacts, regenerated by scripts/compile_paper.py.
# main.pdf is deliberately NOT ignored: it is the point of the example.
*.aux
*.log
*.blg
*.bbl
*.fls
*.fdb_latexmk
*.out
submission.tar.gz
notes/arxiv-registry.sqlite3
